---
tipo: fonte
status: atual
atualizado: 2026-08-23
---

# Fonte: .env.example

## Origem

`/home/gaab/Documentos/gitHub/Xray-Spec/.env.example`

## Conteúdo relevante

- lista de provedores suportados;
- definição de `XRAY_LLM_PROVIDER` e aliases;
- chaves por provider;
- modelo padrão por provider;
- configurações de `CORS`, `RATE_LIMIT` e `TIMEOUT`;
- `XRAY_CURSOR_MODE` e `XRAY_CURSOR_CWD`.

## Uso na wiki

Serve como a fonte de configuração operacional do sistema e de aspeto crítico para o funcionamento real em ambiente local.

## Observação

Este é o melhor artefacto para confirmar a matriz de provedores e as variáveis de ambiente do projecto.
